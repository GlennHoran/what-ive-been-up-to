{
  "name": "whats-glenn-up-to",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy-react" : "aws s3 sync dist/ s3://ghoran-test-deploy-bucket --acl public-read",
    "deploy-serverless" : "aws s3 sync dist/ s3://ghoran-test-deploy-bucket --acl public-read"

  },
  "license": "MIT",
  "dependencies": {
    "components": "^0.1.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.0"
  },
  "devDependencies": {
    "@babel/core": "^7.8.7",
    "babel-preset-nano-react-app": "^0.1.0",
    "parcel-bundler": "^1.12.4"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ],
    "plugins": [
      [
        "@babel/plugin-proposal-class-properties",
        {
          "loose": true
        }
      ],
      [
        "@babel/plugin-transform-react-jsx",
        {
          "pragmaFrag": "React.Fragment"
        }
      ]
    ]
  }
}
